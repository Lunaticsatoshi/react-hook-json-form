import React, { FC } from 'react';
import { IBaseFieldProps } from '../../lib/types';
export declare type DefaultTemplateProps = {
    classNames: string;
    children?: React.ReactNode;
    hidden?: boolean;
};
declare const SchemaField: FC<IBaseFieldProps>;
export default SchemaField;
